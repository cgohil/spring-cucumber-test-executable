package com.test.cucumber.steps;

import com.test.cucumber.config.TestConfig;
import org.springframework.boot.test.context.SpringBootTest;

@SpringBootTest(classes = TestConfig.class)
public class Step {
}
